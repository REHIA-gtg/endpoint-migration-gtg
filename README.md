# endpoint-migration-gtg


used for profwiz corporate deployment for REHIA endpoint migration

must pull these files to C:\workspace locally

running a script to create C:\workspace, deploying these files to said location, then executing script that uses the root location for profwiz.exe + config file + ppkg file + object ID xml file to execute the migration
